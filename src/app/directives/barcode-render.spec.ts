import { BarcodeRender } from './barcode-render';

describe('BarcodeRender', () => {
  it('should create an instance', () => {
    const directive = new BarcodeRender();
    expect(directive).toBeTruthy();
  });
});
